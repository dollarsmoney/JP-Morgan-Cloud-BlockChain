apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction
  namespace: blockchain-fintech
  labels: { app: transaction, app.kubernetes.io/part-of: blockchain-fintech }
spec:
  replicas: 2
  selector:
    matchLabels: { app: transaction }
  template:
    metadata:
      labels: { app: transaction }
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '5055'
        prometheus.io/path: '/metrics'
    spec:
      serviceAccountName: bcf-app
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: transaction
          image: ECR_REGISTRY/bcf-transaction:latest
          imagePullPolicy: IfNotPresent
          ports:
            - { name: grpc, containerPort: 50055 }
            - { name: http, containerPort: 5055 }
          envFrom:
            - configMapRef: { name: bcf-config }
            - secretRef: { name: bcf-secrets }
          readinessProbe:
            httpGet: { path: /readyz, port: 5055 }
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 5055 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: '100m', memory: '192Mi' }
            limits: { cpu: '500m', memory: '512Mi' }
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ['ALL'] }
---
apiVersion: v1
kind: Service
metadata:
  name: transaction
  namespace: blockchain-fintech
  labels: { app: transaction }
spec:
  type: ClusterIP
  selector: { app: transaction }
  ports:
    - { name: grpc, port: 50055, targetPort: grpc }
    - { name: http, port: 5055, targetPort: http }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: transaction
  namespace: blockchain-fintech
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: transaction }
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }
    - type: Resource
      resource: { name: memory, target: { type: Utilization, averageUtilization: 80 } }
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
